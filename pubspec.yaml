name: atelerix_flutter
description: "The official Flutter SDK for Atelerix — unified error tracking, analytics, push notifications, in-app messaging, and user feedback in a single integration."
version: 0.0.1-beta.2
homepage: https://atelerix.dev

environment:
  sdk: '>=3.3.3 <4.0.0'
  flutter: '>=3.3.0'

dependencies:
  flutter:
    sdk: flutter
  package_info_plus: ^8.0.0
  http: ^1.2.1
  plugin_platform_interface: ^2.0.2
  stack_trace: ^1.11.1
  logger: ^2.3.0
  flutter_secure_storage: ^9.2.4
  dio: ^5.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # This section identifies this Flutter project as a plugin project.
  # The 'pluginClass' specifies the class (in Java, Kotlin, Swift, Objective-C, etc.)
  # which should be registered in the plugin registry. This is required for
  # using method channels.
  # The Android 'package' specifies package in which the registered class is.
  # This is required for using method channels on Android.
  # The 'ffiPlugin' specifies that native code should be built and bundled.
  # This is required for using `dart:ffi`.
  # All these are used by the tooling to maintain consistency when
  # adding or updating assets for this project.
  plugin:
    platforms:
      android:
        package: com.atelerix
        pluginClass: AtelerixPlugin
      ios:
        pluginClass: AtelerixPlugin